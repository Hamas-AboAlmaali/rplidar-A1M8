after making a workspace put these files in src folder, after that put all folders in one folder called hector_slam except the folder which called rplidar_ros.
so now you have two folders called hector_slam and rplidar_ros and these two folder are in src folder
